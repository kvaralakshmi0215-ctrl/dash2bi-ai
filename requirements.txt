# Intentionally NOT pinned to exact versions. Streamlit Cloud's Python
# runtime version can change without notice, and exact old pins (e.g.
# streamlit==1.38.0, pandas==2.2.3) have repeatedly failed to build here
# because their transitive dependencies (pillow, numpy, lxml) have no
# prebuilt wheel for newer Python versions and fail compiling from source
# (missing system headers like zlib/libxml2 that aren't installed on the
# host). Lower-bound-only constraints let the resolver pick current
# releases that DO ship prebuilt wheels for whatever Python is active.
streamlit>=1.38
pandas>=2.2.3
numpy>=1.26
openpyxl>=3.1.5
xlrd>=2.0.1
beautifulsoup4>=4.12.3
esprima>=4.0.1
pydantic>=2.9.2
pydantic-settings>=2.5.2
anthropic>=0.34.2
